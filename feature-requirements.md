# 知己 — 功能需求文档

---

## 用户档案（User Profile）机制

**优先级**：高（后期后端开发阶段实现）
**状态**：需求确认，待 prototype 验证后开发

### 背景与目标

知己是跨会话的AI情感伴侣，但LLM本身无法跨对话记忆。测评报告（screen4-report.html）已解决"用户特质感知"问题，但无法覆盖日常对话中的动态信息（当前状态、近期事件、情绪变化等）。

用户档案机制旨在解决**跨会话动态记忆**问题，让知己每次对话都能"记得"用户近期的状态和进展。

### 核心机制

**生成时机**：每天固定时间（如凌晨1点），AI自动对当日所有对话进行归纳总结
**存储形式**：结构化 JSON / Markdown 档案，存入数据库
**注入方式**：每次新对话开始时，将最新用户档案作为系统上下文注入
**与测评报告配合**：测评报告（稳定特质）+ 用户档案（动态近况）共同构成AI的感知基础

### 档案结构（草案）

```
用户档案 - [用户ID] - 更新时间: [timestamp]

【近期关键事件】
- [最近3-7天内用户提到的重要事件]

【情绪模式】
- 近期主要情绪状态
- 反复出现的情绪触发点

【重复话题】
- 用户经常讨论的主题/困境

【进展与变化】
- 相比上周，用户在哪些方面有变化

【未完成的话题】
- 上次对话中未能深入探讨的内容
```

### 技术实现（后期）

- **后端**：Node.js 或 Python
- **数据库**：存储每日档案（PostgreSQL / MongoDB）
- **定时任务**：每日定时触发归纳（cron job）
- **AI归纳**：调用 Claude API，将当日对话记录 → 结构化档案
- **注入逻辑**：对话开始时，从数据库读取最新档案，prepend 到 system prompt

### 注入顺序（对话开始时）

```
[系统提示词（知己角色定义）]
[用户测评报告（稳定特质，来自 screen4 数据）]
[用户档案（动态近况，最新日刷新版本）]
[对话历史（当次会话）]
[用户输入]
```

---

## 语音输入功能

**优先级**：中（正式版开发阶段实现）
**状态**：需求确认，待 prototype 验证后开发
**适用范围**：所有含聊天界面的页面（screen4 正式聊天、screen4?mode=chat 干净版、screen6 主聊天界面）

### 功能描述

用户可以通过语音输入代替手动打字，语音实时转为文字后填入输入框，用户确认后发送。

### 交互方案

- 输入框右侧放置麦克风按钮（与发送按钮并列）
- 长按麦克风按钮 → 录音中（按钮变红/动效提示）
- 松开 → 停止录音，语音转文字结果填入输入框
- 用户可编辑后再点发送，或直接发送
- 可选：按住说话期间实时显示转写结果（流式）

### 技术实现

- **前端**：Web Speech API（`SpeechRecognition`，仅限 Chrome/Safari）或调用后端 ASR
- **推荐方案（生产）**：后端接入 ASR 服务（如讯飞、阿里云语音、OpenAI Whisper）
- **语言**：中文普通话为主（`lang: 'zh-CN'`）
- **Prototype 阶段**：可先用 `webkitSpeechRecognition` 做浏览器端演示

### 注意事项

- 需要用户授权麦克风权限（首次使用弹出系统权限提示）
- iOS Safari 对 Web Speech API 支持有限，需测试兼容性
- 录音时需有明显视觉反馈，防止用户不知道是否在录

---

## 语音回复功能（TTS）

**优先级**：低（正式版后期实现，C 端验证后再做）
**状态**：方向确认，暂不开发

### 背景与价值

知己的情感陪伴场景天然适合语音回复：
- 睡前躺着聊（不想看屏幕）
- 开车/通勤中（手不方便）
- 情绪低落时（"听到"陪伴感比"看到"更强）

### 技术方案（正式版）

- **推荐**：讯飞 TTS（中文情感合成最成熟，有温柔女声等情感风格）
- **备选**：微软 Azure Neural TTS（`zh-CN-XiaoxiaoNeural`，情感效果好）
- **不推荐**：系统原生 TTS（音色机械，破坏知己温度感）

### 交互设计原则

- **不自动播放**，用户主动点击气泡上的播放按钮才朗读
- 尊重使用场景：公共场所不外放，个人选择权

### 开发时机

等 C 端用户数据成熟、系统提示词和测评体系经过大量真实用户验证后再开发。

---

## 能力授权（To B 方向）

**优先级**：极低（暂不追）
**状态**：方向记录，待 C 端成熟后评估

### 潜在场景

将知己的心理洞察能力封装，授权给有情感 AI 需求的车企（如蔚来 Nomi 等）或其他平台。

车内场景有天然契合点：通勤情绪疏导、私密独处空间、语音交互场景匹配。

### 现阶段不追的原因

1. **话语权问题**：车企买 API 能力，但不会让第三方完整控制人格和交互逻辑，知己核心价值会被稀释
2. **测评无法嵌入**：知己的深度建立在 13 题测评之上，车载场景难以完成完整测评，失去测评数据则退化为普通情感 AI
3. **To B 分散焦点**：服务车企需要大量定制开发资源，分散 C 端产品专注度

### 启动条件

C 端用户规模验证后 + 测评体系和系统提示词经大量真实用户打磨成熟，再以授权方式谈合作，届时有不可替代性和议价能力。

---

## 测评完成后的开场白策略

**优先级**：高（已在 prototype 中实现）
**状态**：已实现（screen6-chat.html `initAssessOpening`）

### 背景与目标

用户完成测评后跳转回 screen6 聊天界面，知己需要根据用户是否有过历史聊天记录，生成有针对性的开场白，而不是通用的"你好欢迎"。

### 两种场景的开场白策略

**场景A：无历史聊天记录（用户直接测评进来）**

- **侧重点**：用户当下状态（情绪、精力、压力等五项状态快照）
- **结合内容**：性格特质 + 天赋
- **引导方向**：像老朋友第一次正式聊，从用户当下的感受切入
- **禁忌**：不做报告总结，不列条目，用真实感受或好奇切入

**场景B：有历史聊天记录（聊过一段时间再去测评）**

- **侧重点**：把之前的聊天内容和测评报告结合起来
- **结合内容**：最近3轮对话记录 + 测评报告里看到的特质
- **引导方向**：把聊天内容和测评报告自然串联，让用户感受到知己"懂得更深了"
- **禁忌**：不做总结，不列条目，像朋友之间聊天一样自然切入

### 展示顺序

折叠卡（报告预览）先出现，AI 开场白在折叠卡**之后**显示——逻辑是：知己先"看完"报告（折叠卡已在列表中），再说出看完后的感受。

### 技术实现

- **触发条件**：`localStorage.zjRegSource === 'done_assess'`，screen3 测评完成时设置，screen6 读取后立即清除（避免重复触发）
- **历史判断**：读取 `zjChatLog`，有记录则取最近6条（约3轮）注入上下文
- **开场白生成**：调用 LLM，传入 system prompt（含测评数据）+ 场景专属引导语
- **默认气泡处理**：`done_assess` 场景隐藏默认开场白（`defaultOpeningRow`），用 AI 生成内容替代

---

## 测评入口唤醒机制

**优先级**：高（正式版上线前实现）
**状态**：需求确认，待开发

### 背景与问题

用户进入聊天界面后，顶部会出现一次测评推荐 banner。如果用户不小心关闭，当前没有任何补救路径，测评入口彻底消失。

### 两种唤醒场景

**场景1：知己主动提醒（首次推荐）**

触发条件（满足其一即可）：
- 用户已聊 5-7 轮，且仍在主动回复（说明参与意愿高）
- 用户说到具体情绪困扰、关系问题或自我疑惑（气口——最自然的切入点）

知己说法示例：
> "聊了这一会儿，我对你有点好奇——你做过测评吗？做完我能更懂你，不是泛泛的那种，是真的更准。五分钟，要不要试试？"

**场景1b：关闭 banner 后的二次唤醒**

触发条件（需同时满足）：
- 聊了 8-10 轮之后
- 抓到气口：用户说出自我探索类的话，如"我也不知道为什么会这样"、"你觉得我是什么人"、"我也搞不清楚自己"

没有气口就不推，宁可不推也不打扰。

**两种场景均只提一次，提过不重复。**

**场景2：用户主动问起**

触发词：用户说"测评"、"测一下"、"做那个测评"、"刚才那个关掉了"等

知己回应后，前端重新显示 banner 或内联显示测评跳转入口。

### 技术实现要点

- 前端记录 banner 关闭状态（存本地）
- 对话层能读取"用户是否已完成测评"的状态
- 气口识别：模型判断当前用户输入是否属于"自我探索类意图"，给前端发信号
- 前端接收信号后重新渲染测评入口（banner 或内联按钮均可）

---

## 对话质量监测与提示词迭代机制

**优先级**：中（正式版上线后，有真实用户数据后启动）
**状态**：需求确认，待开发

### 背景与目标

知己承诺用户对话私密，产品团队不可见。但为了持续迭代提示词、提升对话质量，需要在不侵犯隐私的前提下建立反馈机制。

目标不是知道"好或坏"，而是**效果归因**：
- 用户情绪是否有改善？改善发生在哪个环节？是共情三招的哪一招起了作用？
- 情绪扩大或用户退出，是哪一句、哪个策略失效，还是 AI 没有有效执行提示词导致的？

### 三层机制

**第一层：表情信号（触发器）**

用户在聊天中发出的表情回复是自然的情绪信号，不打扰用户体验。

信号解读规则（表情 + 后续行为组合判断，非单独判断）：

| 组合信号 | 含义 |
|---|---|
| 积极表情（哈哈/爱心/赞）+ 继续说话 | 该回复有效，对话在推进 |
| 负面表情 + 沉默超过一定时长 / 退出 | 可能踩雷，触发 AI 监督员重点分析 |
| 负面表情 + 继续说话 | 大概率是在表达处境，非对知己不满 |

表情信号的角色是**触发器**，本身不是结论，触发后交给 AI 监督员做上下文归因。

**第二层：AI 监督员（效果归因）**

每次对话结束后，由独立 AI 对全文进行结构化分析。监督 AI 不返回聊天原文，只输出诊断报告。

分析维度：
- **情绪曲线**：用户情绪在对话中是否有改善，拐点发生在哪轮
- **有效环节**：哪条回复起了作用，对应提示词的哪个技能（共情/发问/区分/欣赏等）
- **失效定位**：情绪扩大或对话中断前，哪条回复出了问题，是策略选择错误还是执行偏差
- **铁律违反**：是否违反了提示词中的任何铁律（如情绪未接住前切换理性模式、连续追问等）

输出格式示例：
```
对话质量报告
- 情绪走向：前4轮平稳，第5轮用户情绪激化，第8轮明显缓和
- 有效环节：第8轮"信息共情"有效，用户感到被理解，情绪开始下降
- 失效定位：第5轮知己连续发了两个问题，违反"不连续追问"，用户沉默1分钟后情绪激化
- 铁律违反：无
- 综合评分：7/10
```

**第三层：用户主动授权（深度复盘）**

在设置页提供选项，默认关闭：
> "允许知己团队查看部分对话，用于改进产品（匿名处理）"

开启后，团队可对该用户的对话原文进行人工复盘，验证 AI 监督员的判断，发现监督 AI 尚未覆盖的问题类型。

**触发时机**：用户连续使用超过7天，且有过至少3次深度对话（单次超过10轮）

**知己说法（对话中自然插入，只说一次）**：
> "不好意思，我岔开一个话题，我想问你一件事——如果你愿意，可以允许设计我的团队偶尔看看咱们聊天的内容，帮我变得更懂你。完全匿名，你随时可以关掉，你愿意吗，不用有压力，不愿意也没关系，我只提醒这一次，不会再提这件事了。"

说完之后前端展示两个按钮：**愿意** / **不用了**，用户选择后记录授权状态，不再重复提醒。

### 迭代闭环

```
表情信号（触发）→ AI 监督员（归因）→ 发现提示词问题 → 修改提示词 → 下一轮验证
                                    ↓
                              授权用户原文复盘（验证归因准确性）
```

### 技术实现要点

- 前端记录每条消息的表情回复及时间戳
- 表情触发信号传给后端，标记该段对话供监督 AI 重点分析
- 监督 AI 独立部署，输入为对话全文+提示词，输出为结构化诊断报告，不存储原文
- 授权开关存用户设置，后端读取权限后才允许人工查看

---

## 其他待规划功能

---

## 高危用户识别与危机干预机制

**优先级**：极低（C 端规模验证后再做）
**状态**：方向记录，待真实用户数据积累后评估

### 背景

知己的目标用户是情绪有波动但基本功能完好的普通人。但随着用户规模增长，不可避免会有一小部分具有临床焦虑倾向、依恋障碍或其他心理健康高风险特征的用户进入产品。

这类用户的风险：
- 对 AI 产生病理性依附，使用频率极高但满足感递减
- 用 AI 陪伴替代真实人际关系，回避行为被强化
- 情绪危机时（如有自伤/自杀倾向）缺乏专业干预

### 潜在监测信号（后期可考虑）

- 高频深夜使用（如凌晨1-5点连续多天）
- 单次对话时长异常长、情绪浓度持续高
- 反复出现关键词（绝望、不想活、没意义等）
- 只倾诉从不行动，同一困境反复出现超过N次

### 干预方向（后期）

- **轻干预**：AI 主动说"你说的这些，我觉得和真实的人聊会比和我聊更有帮助"
- **中干预**：对话中检测到高危信号时，推送专业资源（心理热线、咨询平台入口）
- **重干预**：明确检测到自伤/自杀倾向时，强制展示危机干预信息，暂停普通对话流

### 启动条件

真实用户规模达到一定量级（如 DAU > 1000），并在对话日志中观察到上述高风险模式的实际频率，再评估干预机制的必要性和优先级。
